import React, { ReactElement } from "react"

interface Props {
  name: "ryad"
}

const ChakraDemo = ({}: Props): ReactElement =>{
  return (
    <div>
      <h1>this is text</h1>
    </div>
  )
}

export default ChakraDemo
